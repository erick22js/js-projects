
function AITemplate(nnin, nnhi, nnou){
	
}

function AIEntity(template){
	
	/**
		Neurons Setup
	*/
	/* Input neurons */
	var neurons_input = new Float32Array(template);
	
	/* Hidden neurons */
	var neuron_hidden = new Array(0);
	
	/* Output neurons */
	var neurons_output = new Float32Array(1);
	
	
	
	
}
